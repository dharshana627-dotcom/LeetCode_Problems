class Solution {
    public void moveZeroes(int[] nums) {
        int index=0,value=0,size=nums.length;
        while(index<size){
            if(nums[index]!=0){
                nums[value]=nums[index];
                value++;
            }
            index++;
        }
        Arrays.fill(nums,value,size,0);
    }
}